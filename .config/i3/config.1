################################
#        i3 Config File        #
#             Made             #
#              By              #
#         Marcelo Alves        #
################################


####### Arquivos inicializados no i3 ########

exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec --no-startup-id nm-applet
exec --no-startup-id mopidy
exec --no-startup-id compton
exec --no-startup-id thunar --daemon
exec --no-startup-id numlockx on
#exec --no-startup-id /usr/lib/xfce4/notifyd/xfce4-notifyd

#############################################

# no title window
# window with bar
new_window pixel 3

# set nitrogen wallpaper at startup
exec nitrogen --restore

# force floating for all new windows
#for_window [class="[.]*"] floating enable

###################

# set win key as $mod
set $mod Mod4

# font for windo titles
font pango:Cantarell 10

# start customized dmenu for launch programs
bindsym XF86Search exec "rofi -show drun"

# rofi alt+tab show
bindsym Mod1+Tab exec rofi -show window

# abrir terminal
bindsym $mod+Return exec xfce4-terminal

# kill janela 
bindsym $mod+Shift+q kill

########################
#   Hotkeys/Atalhos    #
########################

bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q -D pulse sset Master 5%+ unmute
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q -D pulse sset Master 5%- unmute
bindsym XF86AudioMute exec --no-startup-id amixer -q -D pulse sset Master 1+ toggle
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
bindsym XF86MonBrightnessDown exec /home/wick/.config/BrightDown.sh
bindsym XF86MonBrightnessUp exec /home/wick/.config/BrightUp.sh

# bloquear tela
bindsym $mod+x exec betterlockscreen --lock

# recarregar polybar
bindsym $mod+Shift+p exec --no-startup-id ~/.config/polybar/launch.sh

# recarregar o arquivo de configuração
bindsym $mod+Shift+c reload

# reiniciar i3 (preserva a sessão/layout, pode ser usado para atualizar o i3)
bindsym $mod+Shift+r restart

# sair do i3 (logout da sessão X)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Deseja encerrar a sessão?' -b 'Sim' 'i3-msg sair'"
mode "[l]ogout, [d]esligar, [r]einiciar, [s]uspender" { 
	bindsym l exec i3-msg exit
	bindsym d exec systemctl poweroff
	bindsym r exec systemctl reboot
	bindsym s exec systemctl suspend

	bindsym Return mode "default"
	bindsym Escape mode "default"
}
bindsym $mod+Shift+e mode "[l]ogout, [d]esligar, [r]einiciar, [s]uspender"

##############################
#       Window Config        #
##############################

##### Configuração de janelas #####

# espaço entre as janelas
gaps inner 15
gaps outer 15

# remove blue window bar i3 uses
for_window [class="^.*"] border pixel 2

# definir workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# mudar para workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# mover janela para workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split horizontal

# split in vertical orientation
bindsym $mod+v split vertical

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, togglesplit
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# use mouse + $mod to drag floating window to their wanted position
floating_modifier $mod

# redimensionar janelas
mode "resize" {
	bindsym Left resize shrink width 3 px or 3 ppt
	bindsym Down resize grow height 3 px or 3 ppt
	bindsym Up resize shrink height 3 px or 3 ppt
	bindsym Right resize grow width 3 px or 3 ppt

	bindsym Return mode "default"
	bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

####################################
#            Regras i3             #
####################################

for_window [window_role="pop-up"] floating enable, move position center
for_window [window_role="bubble"] floating enable, move position center
for_window [window_role="task_dialog"] floating enable, move position center
for_window [window_role="Preferences"] floating enable, move position center
for_window [window_role="dialog"] floating enable, move position center

assign [class="Chromium"] $ws1
assign [class="^Telegram"] $ws2
assign [class="^Gnome-terminal"] $ws3
assign [class="^Thunar"] $ws4
assign [class="^Ncmpcpp"] $ws5

for_window [class="^Thunar"] floating enable, move position center
for_window [class="^Personalizar"] floating enable, move position center

#######################################
#				      #
#	Testing, be careful!	      #
#				      #
#######################################

###### Cores ######

# class                 border      backgr.     text        indicator   child_border
client.focused          #547980     #547980     #ffffff     #547980     #547980
client.focused_inactive #333333     #132a4c     #ffffff     #484e50     #132a4c
client.unfocused        #5a000000   #5a000000   #888888     #5a000000   #5a000000
client.urgent           #e4b226     #e4b226     #ffffff     #e4b226     #e4b226
client.placeholder      #5a000000   #0c0c0c     #ffffff     #000000     #0c0c0c
client.background       #ffffff

###################

# Abrir Chromium
bindsym $mod+g exec chromium

# Abrir Telegram
bindsym $mod+t exec ./Telegram/Telegram

# Abrir Thunar
bindsym $mod+Ctrl+e exec thunar


