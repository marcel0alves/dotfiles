{
	"layer": "top",
	"modules-left": ["sway/workspaces", "sway/mode"],
	"modules-center": ["clock"],
	"modules-right": ["custom/temp", "network", "backlight", "battery", "custom/alsa"],
	
	"clock": {
		"format": "{:%A, %b %d %H:%M}"
	},

	"custom/temp": {
		"format": " {}",
		"interval": 1,
		"exec": "$HOME/.config/waybar/modules/temp.sh 2>&1 /dev/null"
	},

	"custom/alsa": {
		"format": " {}",
		"exec": "$HOME/.config/waybar/modules/alsa.sh 2>&1 /dev/null"
	},

	"network": {
		"format-wifi": " {essid} {ipaddr}" ,
		"format-ethernet": " {ipaddr}"
	},

	"backlight": {
		"device": "intel_backlight",
		"format": " {percent}"
	},

	"battery": {
		"format": "{capacity}% {icon}",
		"format-icons": ["", "", "", "", ""]
	}    
}
