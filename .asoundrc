defaults.ctl.card 1
defaults.pcm.card 1
defaults.timer.card 1

# Alsaequal
ctl.equal {
	type equal
	controls "/home/unknow/.alsaequal.bin"
}

pcm.plugequal {
	type equal
	slave.pcm "plug:dmix"
}

pcm.!default {
	type plug
	slave.pcm plugequal
}
